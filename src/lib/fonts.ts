/**
 * JS-side handles for the typefaces declared in src/app/globals.css.
 * Prefer the `font-display` utility class in JSX; reach for these only where
 * a plain string is required (inline styles, chart configs).
 */
export const FONT_DISPLAY = "var(--font-display)";
export const FONT_SANS = "var(--font-sans)";
