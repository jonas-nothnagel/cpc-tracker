"use client";

/**
 * Constellation centerpiece variant — Phase B implementation.
 *
 * Stubbed in Phase A so the variant picker has something to render and the
 * chip-row tells the team it's coming. The shape is "force-layout nodes in
 * calm space, green arcs for alignment, short red arcs for tension".
 */

import { ComingSoonPlaceholder } from "./placeholder";

export function ConstellationCenterpiece() {
  return (
    <ComingSoonPlaceholder
      name="Constellation"
      tagline="Force-layout night sky. Bright clusters = coherence, dark gaps = uncovered themes."
    />
  );
}
