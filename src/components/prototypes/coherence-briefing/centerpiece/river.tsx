"use client";

/**
 * River braids centerpiece variant — Phase B implementation.
 *
 * Shape: one curved stream per policy document, flowing left to right. Friction
 * markers where streams collide; calm where they flow together.
 */

import { ComingSoonPlaceholder } from "./placeholder";

export function RiverCenterpiece() {
  return (
    <ComingSoonPlaceholder
      name="River braids"
      tagline="One curve per document. Where they flow together, alignment. Where they collide, friction."
    />
  );
}
